import { postAwait } from '@ttkv'
import commonTools from '@/utils/common-tools'

export const page${tableClass.shortClassName}ApiUrl = (params) => {
  return commonTools.getApiBaseUrl('VUE_APP_GZL') + '/back/${tableClass.shortClassName2}/page';
}

